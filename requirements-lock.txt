# Exact versions the reported results were produced with (CPU-only torch).
# Install with:  uv pip install -r requirements-lock.txt -e .
# pyproject.toml keeps the loose lower bounds so the package still installs
# against newer releases; this file is for reproducing the numbers.
joblib==1.6.0
matplotlib==3.11.2
numpy==2.4.6
pandas==3.0.5
pyyaml==6.0.3
scikit-learn==1.9.1
seaborn==0.13.2
torch==2.14.0
tqdm==4.70.1
pytest==9.1.1
ruff==0.16.8
